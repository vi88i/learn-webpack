import { cloneDeep } from "lodash-es";

const downloadPdf = () => {
    const page = {
        description: "Vighnesh Nayak S"
    };

    return cloneDeep(page);
};

export default {
    downloadPdf
};
